"""Protocol entrypoints for the trusted core.

Entrypoints translate authenticated API, web, CLI, worker, and scheduler input
into application commands and queries. They never access persistence directly.
"""

__all__: tuple[str, ...] = ()
